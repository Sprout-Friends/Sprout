import { Search } from 'heroicons-react';
import React from 'react';

const SearchBar = () => {
  return (
    <div id="searchBar" className="form">
      <input type="text" placeholder="Search"></input>
    </div>
  );
};

export default SearchBar;
